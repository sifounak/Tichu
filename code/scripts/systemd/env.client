# Client environment — deploy to /files/.www/tichu/env.client
# Used by tichu-client.service
#
# NOTE: NEXT_PUBLIC_* vars must also be set at build time for Next.js
# to inline them. At runtime only PORT and HOSTNAME are needed here.

NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
